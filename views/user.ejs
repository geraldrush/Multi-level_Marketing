<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Details</title>
    <!-- Include Tailwind CSS from CDN -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 text-gray-900">
    <div class="container mx-auto p-4">
        <h1 class="text-4xl font-bold mb-6 text-center">User Details</h1>
        <ul class="bg-white p-6 rounded-lg shadow-md space-y-4">
            <li><strong class="font-semibold">ID:</strong> <%= user.id %></li>
            <li><strong class="font-semibold">Name:</strong> <%= user.name %></li>
            <li><strong class="font-semibold">Email:</strong> <%= user.email %></li>
            <li><strong class="font-semibold">Role:</strong> <%= user.role %></li>
            <li><strong class="font-semibold">Location:</strong> <%= user.location %></li>
            <li><strong class="font-semibold">Orders:</strong> <%= user.orders.length %></li>
            <li><strong class="font-semibold">Payments:</strong> <%= user.payments.length %></li>
            <li><strong class="font-semibold">Agents:</strong> <%= user.agents.length %></li>
            <li><strong class="font-semibold">Notifications:</strong> <%= user.notifications.length %></li>
            <li><strong class="font-semibold">Chats:</strong> <%= user.chats.length %></li>
        </ul>
    </div>
</body>
</html>
