<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Details</title>
    <!-- Include Tailwind CSS from CDN -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        /* Custom CSS for additional styling */
        .user-image {
            width: 150px;
            height: 150px;
            object-fit: cover;
        }
    </style>
</head>
<body class="bg-gray-100 text-gray-900">
    <div class="container mx-auto p-4">
        <h1 class="text-4xl font-bold mb-6 text-center">User Details</h1>
        <div class="bg-white p-6 rounded-lg shadow-md">
            <div class="flex items-center justify-center mb-6">
                <img src="<%= user.image %>" alt="User Image" class="user-image rounded-full mr-4">
                <div>
                    <h2 class="text-2xl font-bold"><%= user.name %></h2>
                    <p class="text-gray-600"><%= user.email %></p>
                </div>
            </div>
            <ul>
                <li class="flex items-center mb-2">
                    <span class="font-semibold mr-2">ID:</span> <%= user.id %>
                </li>
                <li class="flex items-center mb-2">
                    <span class="font-semibold mr-2">Role:</span> <%= user.role %>
                </li>
                <li class="flex items-center mb-2">
                    <span class="font-semibold mr-2">Location:</span> <%= user.location %>
                </li>
                <li class="flex items-center mb-2">
                    <span class="font-semibold mr-2">Orders:</span> <%= user.orders.length %>
                </li>
                <li class="flex items-center mb-2">
                    <span class="font-semibold mr-2">Payments:</span> <%= user.payments.length %>
                </li>
                <li class="flex items-center mb-2">
                    <span class="font-semibold mr-2">Agents:</span> <%= user.agents.length %>
                </li>
                <li class="flex items-center mb-2">
                    <span class="font-semibold mr-2">Notifications:</span> <%= user.notifications.length %>
                </li>
                <li class="flex items-center mb-2">
                    <span class="font-semibold mr-2">Chats:</span> <%= user.chats.length %>
                </li>
            </ul>
        </div>
    </div>
</body>
</html>
